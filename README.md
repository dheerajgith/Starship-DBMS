# Starship-DBMS
A full‑stack database web application for managing SpaceX Starship missions, crew, payloads, launch sites, and maintenance records.   Built with **Flask** (backend), Oracle Database (via `python-oracledb`), and vanilla HTML/CSS/JS (frontend).
